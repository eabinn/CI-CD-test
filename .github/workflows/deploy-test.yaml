name: Deploy TEST

on:
  workflow_dispatch:
    inputs:
      parameter:
        description: My Parameter

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "HELLO"
          echo "GITHUB_REF: $GITHUB_REF"
