name: Deploy TEST

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Relese tag'
        required: true

      project:
        description: "Project"
        required: true
        type: choice
        options:
          - enterprise
          - multi-tenant


jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "HELLO"
          echo "GITHUB_REF: $GITHUB_REF"
          echo ${{ inputs.tags }}
